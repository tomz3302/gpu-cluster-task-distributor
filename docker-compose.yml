version: '3.8'

services:
  haproxy:
    image: haproxy:latest
    container_name: gpu-load-balancer
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    ports:
      - "8080:8080"  # Frontend for requests
      - "8404:8404"  # Stats dashboard
    restart: unless-stopped
