FINAL PERFORMANCE REPORT
========================================
Total Requests: 1000
Successful:     164
Failed:         836
Target RPS:     100
Actual RPS:     14.58
Total Time:     68.60 seconds
Avg Latency:    15.312 seconds
========================================



The results you are seeing are actually perfectly accurate for the physical limits we programmed into the simulation.
  Your system is behaving exactly as a real, overloaded GPU cluster would!

  Here is the exact breakdown of why you got 164 successes and 836 failures:

  1. The Math of Your Bottleneck
  In our current code:
   * Concurrency Limit: 2 requests per worker.
   * Inference Delay: ~1.5 seconds average (random.uniform(0.5, 2.5)).
   * Worker Throughput: Because each worker can only handle 2 requests at a time, and they take 1.5s each, a single
     worker processes about 1.33 requests per second.

  If you were running 4 workers during this test, your total cluster capacity was about 5.3 requests per second (4
  workers × 1.33).

  2. The Client Timeout (Why they "Failed")
  You sent 1000 requests in 10 seconds.
  Since the cluster can only process ~5 requests per second, a massive queue formed immediately.

  In scheduler.py, we set a strict timeout for the client:

   1 self.client = httpx.AsyncClient(timeout=30.0, limits=limits)
  This means your main.py script will only wait 30 seconds for a response before giving up and marking it as a
  "Failure".

  The Outcome:
  At 5.3 requests per second, the system can only process about 160 requests in 30 seconds.
  Your result was 164 Successful. Every request after the 164th one sat in the queue for longer than 30 seconds, hit the
  client timeout limit, and failed!

  3. The High Latency (15.312s)
  The average latency of 15 seconds is the average time those 164 successful requests spent waiting in the queue +
  processing. The very first request took ~1.5s, but the 164th request waited almost exactly 30 seconds before
  finishing. The average of those times is roughly 15 seconds.

  ---

  How to Fix It (Tune the Simulation)

  To get 1000 successes, you need to align your system's capacity with the load. You have three options:

  Option A: Massive Scaling (The Hardware Solution)
  To handle 100 RPS with a 1.5s delay and a concurrency limit of 2, you need mathematically:
  100 RPS / 1.33 worker_throughput = 75 Workers
  Run: docker-compose up -d --scale worker=75

  Option B: Increase GPU Capacity (The Software Solution)
  Edit worker.py and increase the CONCURRENCY_LIMIT from 2 to something like 50 or 100. This simulates buying GPUs with
  massive VRAM that can batch 100 requests at once.

  Option C: Relax the Timeouts (The Queue Solution)
  If you don't care how long it takes, just let them wait in the queue!
  Edit scheduler.py and remove or massively increase the timeout:

   1 # Wait up to 5 minutes for a response
   2 self.client = httpx.AsyncClient(timeout=300.0, limits=limits)
  (Note: You would also need to increase timeout server 50s to 300s in haproxy.cfg so HAProxy doesn't drop them)